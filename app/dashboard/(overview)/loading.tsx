import DashboardSkeleton from '@/app/ui/skeletons';

/**
 * Renders a loading indicator for the dashboard overview.
 *
 * @returns The loading indicator component.
 */
export default function Loading() {
  return <DashboardSkeleton />;
}
